---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pools.libvirt.karelvanhecke.com
spec:
  group: libvirt.karelvanhecke.com
  names:
    kind: Pool
    listKind: PoolList
    plural: pools
    singular: pool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              hostRef:
                properties:
                  name:
                    pattern: ^[a-z0-9][a-z0-9\-.]{0,251}[a-z0-9]|[a-z0-9]$
                    type: string
                required:
                - name
                type: object
              name:
                type: string
              uuid:
                type: string
            required:
            - hostRef
            type: object
            x-kubernetes-validations:
            - message: at least the name or uuid need to be provided
              rule: '!has(self.name) ? has(self.uuid) : true'
          status:
            properties:
              active:
                type: boolean
              capacity:
                properties:
                  allocation:
                    format: int64
                    type: integer
                  available:
                    format: int64
                    type: integer
                  capacity:
                    format: int64
                    type: integer
                required:
                - allocation
                - available
                - capacity
                type: object
              identifier:
                properties:
                  name:
                    type: string
                  uuid:
                    type: string
                required:
                - name
                - uuid
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
